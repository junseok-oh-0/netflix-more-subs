// Only class names Netflix has kept stable across UI deploys. Hashed ltr-* names rotate on every
// deploy and must never be added here.
export const WATCH_VIDEO = '.watch-video';
export const TIMEDTEXT = '.player-timedtext';
export const TEXT_CONTAINER = '.player-timedtext-text-container';
