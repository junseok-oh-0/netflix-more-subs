# Phase 7 — 스모크 테스트 자동화

상태: **1단계 완료** · 2026-09-16 · 2단계(Playwright) 계획만

## 산출물
| 파일 | 역할 |
|---|---|
| `scripts/smoke/page-checks.js` | 페이지 안에서 실행되는 자립형 검사 함수 `runPageChecks(document, expected?)` |
| `scripts/smoke/snippet.mjs` | 검사 함수를 주입용 스니펫으로 출력 (`npm run smoke:snippet -- --define` / `-- '{...}'`) |
| `test/page-checks.test.js` | 검사 함수를 가짜 플레이어에서 검증 (3개) |
| `.claude/skills/netflix-smoke/SKILL.md` | **`/netflix-smoke`** — 절차 전체를 스킬로. 새 세션·다른 모델에서 재현 가능 |
| `docs/SMOKE_AUTOMATION.md` | 원칙·구성·검사 ID↔체크리스트 대응·1단계 절차·2단계 계획 |

## 원칙 (사용자 결정 2026-09-16)
브라우저 번역기는 검증 대상이 아니다. 두 컨테이너에 같은 원문이 떠도 되며, 확장이 책임지는 것은 `translate` 플래그(원본 `no`, 미러 `yes`), 미러링, 실측 배치뿐. 이로써 "항상 번역" 설정 전제가 사라졌다.

## 첫 실행 결과 (2026-09-16, 빌드 `41675ce` TS 번들)
타이틀 `80014298` @22:00 → 자동재생 → `80014299` → 재진입. 창 1920×1050 / 1000×700.

| 체크리스트 | 결과 | 근거 |
|---|---|---|
| A1~A4 플레이어·컨테이너·플래그 | 통과 | 미러 1개, `translate` 원본 no / 미러 yes |
| A-3 콘솔 에러 | 통과 | 확장 관련 에러 0건 (3회 조회) |
| B-1 자막 흐름 | 통과 | 15초 5회 전환 전부 미러 일치, 잔상 0 |
| B-3 두 줄 겹침 | 통과 | 스택 간격 정확히 8px (4회 측정 모두) |
| B-4 화면 안 | 통과 | 미러 박스가 `.watch-video` 안 |
| C-1 자동재생 | 통과 | 끝 6초 전 seek → Next 버튼 클릭 → 0.8초 후 URL 변경, 캡션 노드 교체, 11초 후 첫 자막 미러링, 미러 1개·스타일 1개 |
| C-2 재진입 | 통과 | 재진입 직후 첫 자막 미러링 |
| F 리사이즈 | 통과 | 원본 54→33px, 미러 32.4→19.8px (비율 0.6 유지), 간격 8px |
| H 옛 UI 없음 | 통과 | |
| E 설정 | 통과 (사용자 판정) | 도구가 팝업을 열 수 없어 반영 검증 절차만 준비. 사용자가 수동 확인 후 통과 처리. E-11 브라우저 재시작은 미실행 |
| G Edge | 미실행 | |

정리: 재생 정지(23:30 지점) → `/browse` 복귀 → 작업 탭 닫음.

## 알려진 제약
- claude-in-chrome 도구는 `chrome-extension://` 페이지를 열 수 없다 → E는 사용자 클릭 + 자동 검증
- Netflix 페이지에서 스크린샷이 자주 타임아웃 → 판정은 JS로만
- C-1은 현재 에피소드를 시청 완료로 기록하는 부수 효과가 있음

## 2단계 (다음)
Playwright + 전용 Chrome 프로필로 `npm run e2e` 터미널 실행. 확장 서비스 워커에서 `chrome.storage.sync.set`을 직접 호출할 수 있어 E까지 완전 자동화됨. `docs/REFACTORING_PLAN.md` Phase 7 참고.
