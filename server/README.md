Local NLLB translation server (FastAPI + CTranslate2). Full usage, env vars, and the API contract
are documented in [`../docs/NLLB_TRANSLATION.md`](../docs/NLLB_TRANSLATION.md).

Quick start: `./run.sh` (uses `~/.venv_global`), then `curl http://127.0.0.1:8008/health`.
